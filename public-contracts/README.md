# HLocation Public Contracts

本目录包含 clean-room 编写的实现无关契约，仅展示产品级数据边界。

## 包含内容

- 通用经纬度值；
- 用户可理解的位置场景描述；
- 场景状态快照；
- 不依赖平台的控制接口。

## 边界

这些文件使用专门设计的公共命名，不复用生产包名、类名或函数名。目录中不包含生产实现、平台接入代码、传输格式、运行配置、内部状态码、安全材料或构建入口。

该契约只用于代码风格和抽象边界展示，不承诺与任一内部版本保持二进制或源码兼容。
